<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %> | Good Nearby</title>
  <meta name="description" content="Good Nearby connects volunteers with local organizations and service projects.">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Fraunces:opsz,wght@9..144,500;9..144,600;9..144,700&family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/main.css">
</head>
<body>
  <a class="skip-link" href="#main-content">Skip to content</a>
  <header class="site-header">
    <div class="wrap site-header__inner">
      <a href="/" class="wordmark">Good Nearby</a>
      <button class="nav-toggle" type="button" aria-label="Toggle menu" aria-expanded="false" aria-controls="primary-nav">
        <span class="nav-toggle__bar"></span>
        <span class="nav-toggle__bar"></span>
        <span class="nav-toggle__bar"></span>
      </button>
      <nav class="primary-nav" id="primary-nav">
        <ul>
          <li><a href="/" class="<%= title === 'Home' ? 'is-active' : '' %>">Home</a></li>
          <li><a href="/organizations" class="<%= title === 'Our Partner Organizations' ? 'is-active' : '' %>">Organizations</a></li>
          <li><a href="/projects" class="<%= title === 'Service Projects' ? 'is-active' : '' %>">Service Projects</a></li>
          <li><a href="/categories" class="<%= title === 'Categories' ? 'is-active' : '' %>">Categories</a></li>
        </ul>
      </nav>
    </div>
  </header>
  <main id="main-content">
